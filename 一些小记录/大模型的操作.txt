第一步：PT预训练 自回归的机器学习 无监督的

第二步：SFT 有监督的微调 
使用有标签的数据来调整一个已预训练好的语言模型（LLM），使其更适应某一特定任务

第三步：PO 偏好对齐 
基于人类反馈的强化学习SLRH
直接偏好对齐 DPO                                 

SFT阶段的操作选择
模式一:基于base模型+领域任务的SFT;
模式二:基于base模型+领域数据 continue pre-train +领域任务SFT;
模式三:基于base模型+领域数据 continue pre-train +通用任务SFT+领域任务SFT;
模式四:基于base模型+领域数据 continue pre-train +通用任务与领域任务混合SFT;

模式五:基于base模型+领域数据 continue pre-train(混入SFT数据) +通用任务与领域任务混合SFT;
模式六:基于chat模型+领域任务SFT;

模式七:基于chat模型+领域数据 continue pre-train +领域任务SFT

a.是否需要continue pre-train
  ①大模型的知识来自于pre-train阶段，如果你的领域任务数据集与pre-train的数据集差异较大，(比如领域任务数据来自公司内部，pre-train训练样本基本不可能覆盖到)那一定要进行continue pre-train。
  ②如果你的领域任务数据量较大(token在1B以上)，并只追求领域任务的效果，不考虑通用能力，建议进行continue pre-train。

b.是选择chat模型 还是base模型
  ①如果有好的base模型，在base模型与在chat模型上进行领域数据的SFT，效果上差异不大。在基于chat模型进行领域SFT，会很容导致灾难性遗忘，在进行领域任务SFT之后，模型通用能力会降低，如只追求领域任务的效果，则不用考虑。
  ②如果你的领域任务与通用任务有很大的相关性，那这种二阶段SFT会提升你的领域任务的效果。如果你既追求领域任务的效果，并且希望通用能力不下降，建议选择base模型作为基座模型。在base模型上进行多任务混合训练，混合训练的时候需要关注各任务间的数据配比。

c.其他情况的选择方案
在资源允许的情况下，如只考虑领域任务效果，应选择模式二;在资源允许的情况下，如考虑模型综合能力，选择模式五;在资源不允许的情况下，考虑模式六;

autoDL 四台4090 先租用7-10天  预算2K
社区镜像 llama-factory 
不用webUI,用脚本进行调参
先进行coutinue pre-training 


